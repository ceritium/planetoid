.span-24.last
  %h2
    Feeds
.span-22.append-1.prepend-1.last
  %h3
    Lista completa de los feeds del planeta Flower  
  %table
    %tr
      %th
        Flower
      %th
        Feed
      %th{ :colspan => "2" }
        \&nbsp;
    - @feeds.each do |feed|
      %tr
        %td
          = link_to h(feed.user.name), user_slug_path(feed.user)
        %td
          = auto_link feed.feed_url
        - if admin?
          %td
            = link_to 'Edit', edit_feed_path(feed)
          %td
            = link_to 'Destroy', feed, :confirm => 'Are you sure?', :method => :delete
    - if admin?
      %br
      = link_to 'New feed', new_feed_path
